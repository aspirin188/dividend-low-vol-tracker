# 红利低波跟踪系统 — 产品需求文档 (PRD)

## 极简版

| 项目 | 信息 |
|------|------|
| 产品名称 | 红利低波跟踪系统 |
| 版本 | v6.4 |
| 版本类型 | **极简版** |
| 最后更新 | 2026-03-28 |
| 文档状态 | 评审修订稿 |
| 设计理念 | 第一性原理 + 乔布斯极简原则 |

### v6.3 → v6.4 修订记录

| 修订项 | 内容 | 原因 |
|--------|------|------|
| 🐛 表头显示 bug | 移除 thead 的 `position: sticky` 定位，修复表头与数据列对齐异常 | `.table-card` 的 `overflow-x: auto` 与 sticky 定位冲突，导致表头偏移 |

### v6.2 → v6.3 修订记录

| 修订项 | 内容 | 原因 |
|--------|------|------|
| 🐛 搜索 bug 修复 | 搜索/筛选无结果时不再跳回欢迎页，改为显示"无匹配结果" | 搜索输入任何内容后页面异常回到初始化状态 |
| 列排序功能 | 点击表头可排序，支持升序↔降序切换 | 用户需要按任意维度快速排序查看 |

---

## 1. 产品哲学

```
问：用户真正想要的是什么？
答：找到值得研究的高股息好公司

设计理念：第一性原理 + 乔布斯极简原则
```

---

## 2. 核心功能

```
功能1：一键运行（获取数据）
功能2：查看标的列表（搜索、筛选、排序）
功能3：导出 Excel
```

---

## 3. 两因子评分模型

```
综合评分 = 股息率归一化 × 0.6 + (1 - 波动率归一化) × 0.4

数据口径：
- 股息率：TTM（近12个月）
- 波动率：120日对数收益率，年化 √242

硬性筛选：
- 股息率 ≥ 4%
- 总市值 ≥ 100亿
- 股利支付率 ≤ 150%
- EPS > 0
- 非ST
```

---

## 4. 单页面设计

```
┌──────────────────────────────────────────────────────┐
│  🏠 红利低波跟踪系统              [ 运行 ] [ 导出 ]  │
├──────────────────────────────────────────────────────┤
│  共 108 只标的  |  数据日期：2026-03-28               │
│                                                       │
│  [搜索名称/代码...]                                   │
│  [全部行业 ▾] [全部市值 ▾] [全部股息率 ▾]            │
│                                                       │
├──────────────────────────────────────────────────────┤
│  排名│名称   │代码  │行业│股价│PE│PB│股息率│...      │
│  （点击表头可排序）                                   │
├──────────────────────────────────────────────────────┤
│  1  │建设银行│601939│银行│8.52│5.8│...              │
│  2  │农业银行│601288│银行│5.23│6.2│...              │
│  ...                                                 │
└──────────────────────────────────────────────────────┘
```

---

## 5. 技术方案

### 5.1 技术栈

| 层 | 技术 |
|----|------|
| 后端 | Python 3.9+ / Flask |
| 前端 | HTML + CSS + JavaScript（原生） |
| 数据处理 | pandas, numpy |
| 股票数据 | akshare |
| Excel导出 | openpyxl |
| 数据存储 | SQLite |

### 5.2 数据表结构（15 字段）

```sql
CREATE TABLE stock_data (
    code             TEXT PRIMARY KEY,
    name             TEXT,
    industry         TEXT,
    dividend_yield   REAL,
    annual_vol       REAL,
    composite_score  REAL,
    rank             INTEGER,
    market_cap       REAL,
    payout_ratio     REAL,
    eps              REAL,
    price            REAL,
    pe               REAL,
    pb               REAL,
    data_date        TEXT,
    updated_at       TEXT
);
```

### 5.3 API 列表

| API | 方法 | 说明 |
|-----|------|------|
| `GET /` | GET | 渲染首页 |
| `POST /api/run` | POST | 一键运行 |
| `GET /api/stocks` | GET | 标的列表（支持搜索/筛选） |
| `GET /api/export` | GET | 导出 Excel |

---

## 6. 验收标准

| 验收项 | 标准 |
|--------|------|
| 表格显示 | 表头与数据列正确对齐，无错位 |
| 搜索 | 无结果时显示"无匹配"，不跳回欢迎页 |
| 排序 | 点击表头可排序，箭头指示方向 |
| 筛选 | 行业/市值/股息率三维度筛选 |

---

## 附录：修订历史

| 版本 | 主要变更 |
|------|----------|
| v6.4 | 修复表头 sticky 定位导致的显示异常 |
| v6.3 | 搜索 bug 修复 + 列排序功能 |
| v6.2 | 增加股价/PE/PB/EPS 列 + 市值/股息率筛选器 |
| v6.1 | 数据口径明确 + min-max 归一化 |
| v6.0 | 极简版初始版本 |

---

*文档结束。*
